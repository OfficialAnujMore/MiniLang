{
  var a = 10;
  var b = 3;
  var sum = a + b;
  var diff = a - b;
  var prod = a * b;
  var div = a / b;
  var mod = a % b;

  print('Summation =', sum);   // 13
  print('Difference =',diff);  // 7
  print('Product =',prod);  // 30
  print('Division =', div);   // 3
  print('Modulus =',mod);   // 1
}
